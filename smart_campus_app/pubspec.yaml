name: smart_campus_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  lottie: ^3.0.0
  google_fonts: ^6.1.0
  supabase_flutter: ^2.12.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  google_maps_flutter: ^2.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
